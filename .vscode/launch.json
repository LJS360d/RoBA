// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Go (Debug)",
      "type": "go",
      "request": "launch",
      "program": "${workspaceFolder}",
      "env": {},
      "args": [
        "-rom=test/arm.gba",
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}